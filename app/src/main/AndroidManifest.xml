<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eden.superzol_edenmor" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="SuperZol" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SearchActivity"
            android:label="@string/app_name"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.eden.superzol_edenmor.MainActivity" />
        </activity>
        <activity
            android:name=".Tab1Activity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".Tab2Activity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".ChooseProductActivity"
            android:label="@string/app_name" >
        </activity>
    </application>

</manifest>
